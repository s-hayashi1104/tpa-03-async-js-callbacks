const getRandomDog = require('./helpers/getRandomDog');

/**
 * getTwoRandomDogs
 *
 * getRandomDog を使用して、二匹ドッグオブジェクトを取得して、
 * callback 関数を実行しよう。callback 関数には引数として
 * 二匹のドッグオブジェクトが入っている配列を渡しましょう。
 *
 * 注意：getRandomDog は非同期な処理を行います。
 *      getRandomDog の中身を見てから実装しましょう。
 */
const getTwoRandomDogs = function(callback) {
  //
  // TODO
  //
};

module.exports = getTwoRandomDogs;
